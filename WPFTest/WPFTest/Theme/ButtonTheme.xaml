<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style BasedOn="{StaticResource {x:Type ToggleButton}}"
           TargetType="RadioButton"
           x:Key="MenuButtonTheme">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid VerticalAlignment="Stretch"
                              HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="39*"/>
                            </Grid.ColumnDefinitions>

                            <Border Name="WhiteRect"
                                    Width="5"
                                    Background="White"
                                    Visibility="Hidden"/>

                            <Border Background="{TemplateBinding Background}"
                                    Grid.Column="1"
                                    Name="MainRect">
                                <Border.Clip>
                                    <PathGeometry>
                                        <PathFigure StartPoint="0,0">
                                            <LineSegment Point="0,50"/>
                                            <LineSegment Point="155,50"/>
                                            <ArcSegment Point="175,30"
                                                        Size="20,20"/>
                                            <LineSegment Point="175, 20"/>
                                            <ArcSegment Point="155,0"
                                                        Size="20,20"/>
                                            <LineSegment Point="175,0"/>
                                        </PathFigure>
                                    </PathGeometry>
                                </Border.Clip>

                                <TextBlock VerticalAlignment="Center"
                                       Margin="20, 0 ,0 ,0"
                                       Text="{TemplateBinding Property=Content}"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="MainRect" Property="Background" Value="#22002f"/>
                                <Setter TargetName="WhiteRect" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            
        </Style.Setters>
    </Style>
</ResourceDictionary>